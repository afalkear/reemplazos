<%= form_tag 'activities/add_hour', method: :post, remote: true do %>
  <%= select_tag "hour", options_for_select(@available_hours) %>
  <%= submit_tag "Create", class: "btn btn-default" %>
<% end %>
