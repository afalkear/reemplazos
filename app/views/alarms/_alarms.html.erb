<% alarms.each do |alarm| %>
  (<%= "#{alarm.offset} #{alarm.offset_type}" %> <%= link_to "x", alarm_path(alarm.id), method: :delete, data: { confirm: "Are you sure?" } %>)
<% end %>
<%= link_to "+", new_alarm_path(activity_id: activity_id, responsible_id: responsible_id) %>
